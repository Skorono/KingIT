<Window x:Class="KingIT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns:local="clr-namespace:KingIT"
        xmlns:WpfLibraryAuth="clr-namespace:WpfLibrary.Components.Auth;assembly=WpfLibrary"
        mc:Ignorable="d"
        Title="MainWindow" Height="auto" Width="auto">
    <Grid x:Name="MainGrid" Background="#FFB7B4B4">
        <WpfLibraryAuth:AuthFirstForm x:Name="RegistrationArea"> </WpfLibraryAuth:AuthFirstForm>
        <ListBox x:Name="List"  Width="200" Height="350" HorizontalAlignment="Right">
            
        </ListBox>
        <Frame x:Name="MainFrame"></Frame>
    </Grid>
</Window>
